# book store demo drone github

Run cmd

```
> docker-compose build
> docker-compose up
```

Open browser `http://localhost:8080/auth/admin/`

Add new realm 

Select file `import-keycloak-have-permission.json`

Open browser `http://localhost:8888`